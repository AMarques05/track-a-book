<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book - BookNoteVault</title>
</head>
<body>
    <h1>✏️ Edit Book</h1>
    
    <h2>📖 Book Information (Read-Only)</h2>
    <div style="border: 1px solid #ccc; padding: 15px; background-color: #f9f9f9;">
        <h3><%= book.title || 'Untitled' %></h3>
        <p><strong>Authors:</strong> <%= book.authors || 'Unknown' %></p>
        <p><strong>Published:</strong> <%= book.published_date || 'Unknown' %></p>
        
        <% if (book.thumbnail) { %>
            <img src="<%= book.thumbnail %>" alt="Book cover" style="max-width: 150px; height: auto;">
        <% } %>
        
        <p><strong>ISBN-10:</strong> <%= book.isbn_10 || 'N/A' %></p>
        <p><strong>ISBN-13:</strong> <%= book.isbn_13 || 'N/A' %></p>
    </div>
    
    <hr>
    
    <h2>📝 Edit Your Personal Notes & Rating</h2>
    <form action="/books/edit/<%= book.id %>" method="POST" style="border: 1px solid #ccc; padding: 15px;">
        
        <label for="rating">Your Rating (1-10):</label><br>
        <input type="number" id="rating" name="rating" min="1" max="10" 
               value="<%= book.rating || '' %>" placeholder="Rate from 1 to 10"><br><br>
        
        <label for="date_read">Date You Read This:</label><br>
        <input type="date" id="date_read" name="date_read" 
               value="<%= book.date_read ? book.date_read.toISOString().split('T')[0] : '' %>"><br><br>
        
        <label for="notes">Your Notes:</label><br>
        <textarea id="notes" name="notes" rows="8" cols="50" 
                  placeholder="Write your thoughts, favorite quotes, or any notes about this book..."><%= book.notes || '' %></textarea><br><br>
        
        <button type="submit">💾 Save Changes</button>
    </form>
    
    <hr>
    
    <h3>⚠️ Danger Zone</h3>
    <form action="/books/delete/<%= book.id %>" method="POST" 
          onsubmit="return confirm('Are you sure you want to delete this book? This action cannot be undone!')">
        <button type="submit" style="background-color: #ff6b6b; color: white;">
            🗑️ Delete This Book
        </button>
    </form>
    
    <hr>
    <p>
        <a href="/books">
            <button>← Back to My Books</button>
        </a>
    </p>
</body>
</html>